extends layout 

block content 
  h1= title 

  ul
    each item in type_list 
      li 
        a(href=`/catalog/item/${item.href}`)= item.name
  
    else 
      li There are no items with this type